# Copier vers .env et remplir (le fichier .env ne doit pas être commité).
# Google Cloud Console → APIs & Services → Credentials → client OAuth Web.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optionnel (déjà utilisé ailleurs dans docker-compose)
# SENDGRID_API_KEY=
